name: Build and Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pyinstaller
      - name: Build
        working-directory: packaging
        run: pyinstaller plateau-converter.spec
      - name: Rename
        shell: bash
        run: mv packaging/dist/plateau-converter.exe packaging/dist/plateau-converter-windows.exe
      - uses: softprops/action-gh-release@v2
        with:
          body: |
            コーディングをされない方のために、使いやすくソフト化したものです。
            以下からお使いの環境に合ったファイルをDLしてください。

            **Windows**
            「plateau-converter-windows.exe」をダウンロードしてください。
            ※ ダブルクリックしても開けない場合は、右クリックのプロパティから権限を付与してください。

            **Mac（v1.8 より対応）**
            - Intel Mac：「plateau-converter-macos-intel.zip」
            - Apple Silicon（M1/M2/M3）：「plateau-converter-macos-arm.zip」

            解凍後、.app を右クリック →「開く」で起動してください。
            ※ 「開発元不明」の警告が出た場合は、システム設定 → プライバシーとセキュリティ から「このまま開く」を選択してください。

            **v1.10.1 更新内容**
            - ソフト名・ファイル名の表記を正しい綴り「plateau」に統一（旧: plateu）。
          files: packaging/dist/plateau-converter-windows.exe

  build-mac-arm:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pyinstaller
      - name: Build
        working-directory: packaging
        run: pyinstaller plateau-converter.spec
      - name: Package .app
        run: |
          APP="packaging/dist/plateau-converter.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          cp packaging/dist/plateau-converter "$APP/Contents/Resources/plateau-converter"
          chmod +x "$APP/Contents/Resources/plateau-converter"
          cp packaging/mac-launcher.sh "$APP/Contents/MacOS/plateau-converter"
          chmod +x "$APP/Contents/MacOS/plateau-converter"
          cp packaging/mac-info.plist "$APP/Contents/Info.plist"
          cd packaging/dist && zip -r plateau-converter-macos-arm.zip plateau-converter.app
      - uses: softprops/action-gh-release@v2
        with:
          files: packaging/dist/plateau-converter-macos-arm.zip

  build-mac-intel:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Install Python 3.11 universal2 (python.org)
        run: |
          curl -LO https://www.python.org/ftp/python/3.11.9/python-3.11.9-macos11.pkg
          sudo installer -pkg python-3.11.9-macos11.pkg -target /
      - name: Install PyInstaller (x86_64)
        run: arch -x86_64 /Library/Frameworks/Python.framework/Versions/3.11/bin/pip3 install pyinstaller
      - name: Build (x86_64)
        working-directory: packaging
        run: arch -x86_64 /Library/Frameworks/Python.framework/Versions/3.11/bin/python3.11 -m PyInstaller plateau-converter.spec
      - name: Package .app
        run: |
          APP="packaging/dist/plateau-converter.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          cp packaging/dist/plateau-converter "$APP/Contents/Resources/plateau-converter"
          chmod +x "$APP/Contents/Resources/plateau-converter"
          cp packaging/mac-launcher.sh "$APP/Contents/MacOS/plateau-converter"
          chmod +x "$APP/Contents/MacOS/plateau-converter"
          cp packaging/mac-info.plist "$APP/Contents/Info.plist"
          cd packaging/dist && zip -r plateau-converter-macos-intel.zip plateau-converter.app
      - uses: softprops/action-gh-release@v2
        with:
          files: packaging/dist/plateau-converter-macos-intel.zip
