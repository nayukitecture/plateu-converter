# PLATEAU CityGML → OBJ 変換ツール

## 概要

国土交通省 PLATEAU が提供する CityGML データを OBJ 形式に変換する Python スクリプトです。  
ブロック（区画）単位で 1 つの OBJ ファイルを生成し、Rhinoceros などの 3D ソフトに直接読み込めます。  
複数ブロックを指定した場合は 1 つのファイルにまとめて出力します。

---

## データのダウンロード

PLATEAU オープンデータは以下から入手できます。

**https://www.mlit.go.jp/plateau/open-data/**

ダウンロードした ZIP を展開し、`resources/input/` 以下に配置してください。

```
resources/input/
  13209_machida-shi_pref_2023_citygml_2_op/    ← 展開したフォルダをここに置く
  14100_yokohama-shi_city_2024_citygml_2_op/
```

サンプルデータとして `13209_machida-shi_pref_2023_citygml_2_op` の一部ブロックを同梱しています。  
以下のコマンドをそのまま実行して動作確認できます。

---

## 必要環境

- Python 3.8 以上
- 追加ライブラリ不要（標準ライブラリのみ使用）

---

## 使い方

`main-program/` フォルダで実行します。

```bash
python plateu-converter.py -lod 1 -b 53392339 -t bldg -o sample -i "../resources/input/13209_machida-shi_pref_2023_citygml_2_op"
```

詳細はオプション一覧を参照してください。

---

## オプション一覧

| オプション | 取りうる値 | 説明 | デフォルト |
|-----------|-----------|------|-----------|
| `-i <DIR>` | フォルダパス | 入力データセットのルートフォルダ（`udx/<type>` まで自動探索） | （必須） |
| `-lod <N>` | `1` / `2` | LOD レベル | `1` |
| `-b <BLOCK...>` | 数字のブロック番号（スペース区切りで複数可） | 変換するブロック番号。省略時は全ブロック | （全件） |
| `-t <TYPE...>` | `bldg` / `tran` / `luse` / `veg` / `frn`（スペース区切りで複数可） | オブジェクト種別。複数指定すると種別ごとに別ファイルで出力 | `bldg` |
| `-o <NAME>` | 任意の名前（例: `yokohama`, `machida-station`） | `resources/output/` 内に作成するフォルダ名 | `resources/output/` 直下 |

---

## 出力

- **出力先**: `resources/output/<-o で指定した名前>/`
- **ファイル名**: `{-o}_{LOD}_{type}_{ブロック番号...}.obj`
  - 例: `machida-station_LOD2_bldg_53392345_53392355.obj`
- **内部構造**: オブジェクトごとに `o {ID}` で区切られる  
  → Rhinoceros で開いて **Explode** すれば 1 つずつのオブジェクトになります

### 完了時の表示例

**LOD1 の場合**

```
======= bldg / LOD1 / 1 ブロック =======
  53392339_bldg_6697_op.gml: 12 棟
  総数     : 12 棟
========================================

  出力先   : .../resources/output/sample/
========================================
========================================
```

**LOD2 の場合（複数タイプ・複数ブロック）**

```
======= bldg / LOD2 / 2 ブロック =======
  53392345_bldg_6697_op.gml: 425 棟 (内 LOD1 で代替: 178 棟)
  53392355_bldg_6697_op.gml: 1819 棟 (内 LOD1 で代替: 634 棟)
  総数     : 2244 棟
  LOD2     : 1432 棟
  LOD1代替 : 812 棟（LOD2データなし）
========================================

======= tran / LOD2 / 2 ブロック =======
  53392345_tran_6697_op.gml: 177 件 (内 LOD1 で代替: 74 件)
  53392355_tran_6697_op.gml: 825 件 (内 LOD1 で代替: 266 件)
  総数     : 1002 件
  LOD2     : 662 件
  LOD1代替 : 340 件（LOD2データなし）
========================================

  出力先   : .../resources/output/machida-station/
========================================
========================================
```

---

## 座標系

等距離円筒図法（基準緯度 35°）でメートル変換して出力します。

| 軸 | 方向 |
|----|------|
| X | 東（経度方向） |
| Y | 北（緯度方向） |
| Z | 高さ（m） |

異なる都市・地区のデータも同じ変換式を使うため、そのまま合体できます。

---

## LOD について

| LOD | 形状 | 備考 |
|-----|------|------|
| LOD1 | フットプリントを高さ方向に押し出したボックス | 全建物に存在 |
| LOD2 | 屋根形状あり（切妻・陸屋根など） | データによって整備状況が異なる |

LOD2 を指定してもデータがない建物は自動的に LOD1 で代替します。  
変換完了時に LOD2 / LOD1代替 それぞれの棟数を表示します。
